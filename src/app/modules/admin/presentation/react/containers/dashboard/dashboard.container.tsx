import type React from 'react';
import { DashboardLayout } from '../../layouts/dashboard/dashboard.layout';

export const DashboardContainer = (): React.ReactElement => {
  return <DashboardLayout />;
};
